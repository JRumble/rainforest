<h1>Product</h1> 

<%= link_to 'New Product', new_product_path %>

<% @products.each do |product| %>
	<p><%= product.name %>, <%= product.description %>, <%= product.formatted_price %>
		<%= link_to 'View Product', product_path(product) %></p>	
	
<% end %>
